
<% @posts.each do |p| %>
  <%= image_tag p.image.variant(resize_to_limit: [250, 250]).processed.service_url %>
  <h3><%= link_to p.title, post_path(p.id) %></h3>
  <div><%= p.tag_list %></div>
<% end %>
